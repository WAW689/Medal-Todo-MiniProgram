# 勋章代办需求文档

## 1. 应用概述

- **应用名称**：勋章待办
- **应用描述**：一款注重体验与激励的微信小程序待办事务管理工具。用户可高效管理个人事务与多人协作任务，通过游戏化勋章系统将每次任务完成转化为可视化成就，并支持灵活的标签体系对任务进行分类与筛选，持续激发使用动力。

---

## 2. 用户与使用场景

### 2.1 目标用户
- 有个人任务管理需求、希望通过成就感驱动自我提升的微信用户
- 有团队协作任务管理需求的微信用户

### 2.2 核心使用场景
- 用户创建待办事务，设置截止日期与难度，完成后获得对应稀有度勋章
- 用户发起多人协作任务，邀请成员参与，协同完成任务
- 用户通过勋章墙回顾历史成就，激励持续完成任务
- 用户通过日历视图与时间轴视图掌握任务时间分布
- 用户通过标签对任务进行分类，并按标签筛选查看任务

---

## 3. 页面结构与核心功能

### 3.1 页面总览

```
勋章待办（微信小程序）
├── 主页（首页）
│   ├── 勋章墙区域
│   ├── 标签筛选栏
│   ├── 未完成任务列表（含协作任务标识）
│   ├── 已完成任务列表
│   └── 添加新事务入口
├── 事务创建/编辑页
├── 协作任务详情页
├── 时间视图页
│   ├── 日历视图
│   └── 时间轴视图
├── 勋章详情页
├── 标签管理页
└── 个人中心页（登录态）
```

### 3.2 登录
- 支持微信授权登录
- 未登录用户进入小程序时，弹出微信授权登录引导弹窗
- 授权后获取微信用户基本信息（昵称、头像），完成账号绑定
- 登录成功后进入主页

### 3.3 主页

**勋章墙区域**
- 位于页面顶部核心区域，横向滚动展示用户已获得的所有勋章
- 勋章按获得时间倒序排列，最新获得的勋章置于最前
- 每枚勋章以图标形式展示，图标颜色/光效根据稀有度区分（见勋章设计规则）
- 点击任意勋章跳转至勋章详情页
- 若暂无勋章，展示引导文案：「完成你的第一个任务，解锁专属勋章」

**标签筛选栏**
- 位于勋章墙区域下方、任务列表上方，横向滚动展示所有已创建标签
- 默认选中「全部」，展示所有任务
- 点击某个标签，任务列表仅展示关联该标签的任务
- 支持多标签组合筛选：可同时选中多个标签，列表展示同时关联所有已选标签的任务
- 筛选栏末尾提供「管理标签」入口，点击跳转至标签管理页
- 当前已选标签以高亮色块形式区分未选标签

**未完成任务列表**
- 展示所有未完成的待办事务（含个人任务与协作任务），按截止日期升序排列（最近截止的排在最前）
- 每条任务项展示：任务标题、截止日期、难度标签、关联标签色块列表；协作任务额外展示协作成员头像列表与「协作」标识角标
- 截止日期已过但未完成的任务，截止日期文字标红提示
- 每条任务项右侧设有醒目的「完成」按钮
- 点击任务标题区域：个人任务进入事务创建/编辑页（编辑模式），协作任务进入协作任务详情页
- 标签筛选生效时，列表仅展示符合筛选条件的任务

**已完成任务列表**
- 折叠展示，默认收起，用户可手动展开
- 展示已完成任务的标题、完成时间、获得勋章稀有度标签、关联标签色块列表；协作任务展示「协作」标识
- 不提供再次编辑入口，仅供查看
- 标签筛选生效时，列表仅展示符合筛选条件的任务

**添加新事务入口**
- 页面右下角悬浮「+」按钮，点击跳转至事务创建页

### 3.4 事务创建/编辑页

**字段**
- 事务标题（必填，文本输入，最多50字）
- 详细描述（选填，多行文本，最多200字）
- 截止日期与时间（必填，日期时间选择器）
- 任务难度（必填，单选：简单 / 中等 / 困难）
- 预估耗时（选填，数字输入，单位：小时，用于勋章稀有度计算）
- 关联标签（选填，从现有标签中选择一个或多个；以色块形式展示已选标签；支持快速选择，选择界面直观易用；可在选择界面内快速跳转至标签管理页新建标签）
- 是否开启多人协作（选填，开关切换；开启后显示邀请成员入口）
- 邀请成员（协作模式下可见，通过微信好友选择器选择成员，支持添加多人）

**操作**
- 保存：校验必填项后保存任务，返回主页
- 删除（编辑模式下可见）：二次确认后删除该任务，返回主页
- 取消：放弃当前编辑，返回上一页

### 3.5 协作任务详情页

- 展示任务标题、详细描述、截止日期、难度、预估耗时、关联标签色块列表
- 展示协作成员列表：头像、昵称、各成员完成状态（未完成 / 已完成）
- 创建者可在此页面追加邀请新成员或移除现有成员（任务完成前有效）
- 每位成员可独立点击「完成」标记自己已完成该任务
- 当所有成员均标记完成后，任务整体进入完成状态，所有成员各自获得对应勋章
- 创建者可在此页面删除协作任务（二次确认）

### 3.6 任务完成流程

- 用户点击任务项旁「完成」按钮后触发完成流程：
  1. 任务项播放淡出动画（约600ms）
  2. 弹出勋章获得弹窗：展示本次获得勋章的图标、名称、稀有度、获得条件说明，并播放庆祝动画（粒子/光效）及轻快提示音
  3. 弹窗关闭后，勋章自动归入勋章墙（勋章墙区域播放新勋章入场动画）
  4. 任务从未完成列表移入已完成列表
- 协作任务中，单个成员标记完成后仅触发该成员侧的完成动效与勋章发放；任务整体完成（所有成员均完成）时，所有成员均收到微信订阅消息通知

### 3.7 时间视图页

**日历视图**
- 以月历形式展示，每日格内显示当日任务数量角标
- 点击某日，在日历下方展示该日所有任务列表（标题、难度、完成状态、关联标签色块，协作任务附「协作」标识）
- 点击任务可跳转至对应编辑页或协作任务详情页

**时间轴视图**
- 横向时间轴，以当前日期为基准，向前后各延伸展示任务
- 每条任务以色块形式展示在时间轴上，色块长度对应任务预估耗时（无预估耗时则以固定宽度展示）
- 色块颜色根据任务状态区分：未完成（蓝色）、已逾期（红色）、已完成（灰色）
- 协作任务色块附加「协作」角标
- 点击色块展示任务标题、截止日期与关联标签的浮层提示

### 3.8 勋章详情页

- 展示勋章大图、名称、稀有度、获得时间、获得条件说明
- 稀有度为「史诗」或「传说」的勋章附带动态光效展示

### 3.9 标签管理页

**标签列表**
- 展示用户已创建的所有标签，每条标签展示：颜色色块、标签名称、关联任务数量
- 支持对已有标签进行编辑（修改名称或颜色）和删除操作

**创建标签**
- 提供「新建标签」入口
- 创建时需填写：标签名称（必填，最多10字）、标签颜色（必填，从预设颜色中选择）
- 保存后标签立即可用

**编辑标签**
- 点击标签项进入编辑状态，可修改名称与颜色
- 修改后所有关联该标签的任务同步更新展示

**删除标签**
- 点击删除时，若该标签已关联任务，弹出提示：「删除后，已关联该标签的任务将自动解除关联，是否确认删除？」
- 用户确认后删除标签，所有关联任务自动解除与该标签的关联
- 若该标签未关联任何任务，直接二次确认后删除

### 3.10 个人中心页

- 展示用户微信头像与昵称
- 展示累计完成任务数、累计获得勋章数
- 提供退出登录入口

---

## 4. 业务规则与逻辑

### 4.1 勋章稀有度计算规则

勋章稀有度由以下三项属性综合评分决定，总分对应稀有度等级：

| 属性 | 评分规则 |
|------|----------|
| 任务难度 | 简单 = 1分；中等 = 2分；困难 = 3分 |
| 预估耗时 | 无填写或 < 1小时 = 0分；1–3小时 = 1分；3–8小时 = 2分；> 8小时 = 3分 |
| 是否准时完成 | 在截止日期前完成 = 2分；逾期完成 = 0分 |

**总分与稀有度对应关系：**

| 总分 | 稀有度 |
|------|--------|
| 0–2分 | 普通（灰色，无特效） |
| 3–4分 | 稀有（蓝色，轻微光泽） |
| 5–6分 | 史诗（紫色，流光动效） |
| 7–8分 | 传说（金色，粒子光效 + 动态闪烁） |

- 协作任务中，每位成员的勋章稀有度独立计算，以该成员自身的完成时间（是否准时）为准

### 4.2 勋章命名规则

- 勋章名称由系统根据任务难度与稀有度自动生成，格式示例：
  - 普通：「初心之印」
  - 稀有：「专注之章」
  - 史诗：「破晓勋章」
  - 传说：「传说·极限勋章」
- 每枚勋章附带获得条件说明，格式示例：「准时完成一项困难任务（预估耗时 > 8小时）」

### 4.3 标签规则

- 每个用户的标签数据与账号绑定，存储于后端数据库
- 标签名称在同一用户下不可重复
- 一个任务可关联零个或多个标签
- 多标签组合筛选逻辑为「AND」关系：展示同时包含所有已选标签的任务
- 删除标签后，已关联该标签的任务自动解除关联，任务本身不受影响
- 标签颜色从系统预设颜色集中选择，不支持自定义输入色值

### 4.4 多人协作规则

- 协作任务由创建者发起，创建时通过微信好友选择器邀请成员
- 被邀请成员通过微信订阅消息或小程序内通知收到邀请，进入小程序后可在主页看到该协作任务
- 每位成员独立标记自己的完成状态，互不影响
- 所有成员均标记完成后，任务整体进入完成状态，各成员独立获得勋章
- 创建者可在任务完成前移除成员或追加新成员；被移除成员不再显示该任务，已标记的完成状态随之失效
- 协作任务删除仅创建者有权操作；删除后所有成员的任务列表中同步移除该任务
- 协作任务不支持非创建者编辑任务基本信息（标题、截止日期、难度等）

### 4.5 数据持久化

- 任务数据、勋章数据与标签数据均与微信登录账号绑定，存储于后端数据库
- 用户更换设备登录后，数据保持同步

### 4.6 提醒规则

- 用户创建任务并设置截止日期后，系统在截止日期前1小时通过微信小程序订阅消息推送提醒
- 协作任务整体完成时，所有成员收到微信订阅消息通知
- 提醒需用户授权订阅消息权限

---

## 5. 异常与边界情况

| 场景 | 处理方式 |
|------|----------|
| 用户未授权微信登录 | 弹窗提示「登录后可同步数据，建议授权」，提供「暂不登录」选项，以游客模式使用（数据仅存本地，不跨端同步）；游客模式下不可发起或参与协作任务 |
| 任务标题为空时点击保存 | 标题输入框标红，提示「请填写事务标题」，阻止保存 |
| 截止日期早于当前时间时保存 | 弹出提示「截止时间已过，是否仍要保存？」，用户确认后允许保存，任务标记为逾期状态 |
| 勋章墙无勋章 | 展示空状态引导文案，不展示空白区域 |
| 时间轴无任务数据 | 展示空状态提示「暂无任务，去添加第一个吧」 |
| 网络异常时保存任务 | 提示「网络异常，数据已暂存本地，联网后自动同步」 |
| 用户删除任务 | 二次确认弹窗，确认后删除，已完成任务对应勋章不受影响 |
| 协作任务创建者删除任务 | 二次确认弹窗，确认后删除，所有成员任务列表同步移除，已获得勋章不受影响 |
| 被邀请成员未授权订阅消息 | 协作任务邀请通知不发送，成员进入小程序后仍可在主页看到协作任务 |
| 协作任务仅剩创建者一人（其他成员均被移除） | 任务退化为个人任务，创建者完成后独立获得勋章 |
| 删除已关联任务的标签 | 弹出确认提示，用户确认后删除标签，关联任务自动解除与该标签的关联，任务本身不受影响 |
| 标签名称与已有标签重复 | 保存时提示「标签名称已存在，请修改后重试」，阻止保存 |
| 标签管理页无标签 | 展示空状态提示「暂无标签，点击新建」 |
| 多标签组合筛选无匹配任务 | 任务列表展示空状态提示「暂无符合条件的任务」 |

---

## 6. 验收标准

- 微信授权登录流程完整可用，登录后数据与账号绑定
- 任务创建、编辑、删除功能正常，必填项校验有效
- 点击「完成」按钮后，淡出动画、勋章弹窗、提示音、勋章入场动画依次正常触发
- 勋章稀有度根据任务难度、预估耗时、是否准时完成三项属性正确计算
- 不同稀有度勋章在视觉上有显著区分（颜色、光效）
- 勋章墙正确展示所有已获得勋章，点击可查看详情
- 日历视图正确展示每日任务数量，点击日期可查看当日任务列表
- 时间轴视图正确展示任务时间分布，状态颜色区分准确，协作任务附角标
- 截止日期提醒（订阅消息）在授权后正常推送
- 游客模式下数据本地存储，登录后数据同步至云端
- 动画帧率流畅，完成操作响应时间不超过300ms
- 协作任务创建流程完整，成员邀请、独立完成标记、整体完成触发逻辑正确
- 协作任务中各成员勋章独立发放，稀有度计算以各自完成时间为准
- 创建者移除成员后，被移除成员任务列表同步更新
- 协作任务整体完成后，所有成员收到订阅消息通知
- 标签创建、编辑、删除功能正常，名称重复校验有效
- 任务创建/编辑页可正确关联和展示标签
- 主页标签筛选栏展示所有标签，单标签与多标签组合筛选结果准确
- 删除已关联任务的标签后，相关任务自动解除关联，任务数据不受影响
- 标签数据与账号绑定，更换设备登录后标签数据正常同步

---

## 7. 本期不实现功能

- 自定义勋章图案或上传图片
- 任务重复/周期性设置
- 数据统计报表与趋势分析
- 桌面端（PC）适配
- 第三方日历（如系统日历）同步
- 社交分享勋章至朋友圈/好友
- 标签自定义色值输入（本期仅支持预设颜色选择）
